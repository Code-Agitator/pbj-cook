services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DACOOK_TIMEZONE=Asia/Shanghai
      - DACOOK_SEED_DEV=0
    restart: unless-stopped
